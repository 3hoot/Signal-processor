
#include <cmath>
#include <vector>

#include "generic.h"

std::vector<double> signal::generate(SignalType type, double frequency, double amplitude, double duration, double samplingFrequency)
{
    // placeholder
    return std::vector<double>();
}
